name: Bug Report
description: Create a report to help us improve

body:
  - type: markdown
    attributes:
      value: |
        ⚠️ Make sure to browse the opened and closed issues before submit your issue.

  - type: textarea
    id: sample
    attributes:
      label: "The following program `in.xgo` triggers an unexpected result"
      value: |
        // code to reproduce the bug
      render: coffee
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Expected result
      render: console
    validations:
      required: true

  - type: textarea
    id: got
    attributes:
      label: Got
      description: |-
        ```console
        $ xgo run ./in.xgo
        // output
        ```
      placeholder: $ xgo run ./in.xgo
      render: console
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: XGo Version
      description: Can be a tag or a hash.
    validations:
      required: true

  - type: textarea
    id: additional
    attributes:
      label: Additional Notes
      description: Use [Markdown syntax](https://help.github.com/articles/github-flavored-markdown) if needed.
    validations:
      required: false
