doc := golang`package main

var (
	a, b string
	c    int
)

func add(a, b int) int {
	return a + b
}

func mul(a, b float64) float64 {
	return a * b
}
`!

for fn in doc.decls.*@(self.class == "FuncDecl") {
	echo fn.$name
}
